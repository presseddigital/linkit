{% import "_includes/forms" as forms %}

{{ forms.textField({
    label: "Label"|t('link-it'),
    instructions: "Enter the text you want to appear in the LinkIt dropdown."|t('link-it'),
    id: type.type|id~'-customLabel',
    name: 'typesSettings['~type.type~'][customLabel]',
    value: type.customLabel ?? '',
    placeholder: type.defaultLabel,
    errors: type.getErrors('customLabel')
}) }}
