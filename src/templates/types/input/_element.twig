{% import "_includes/forms" as forms %}

{% set element = (link.element ?? false) ? [link.element] : [] %}

{{ forms.elementSelect({
    id: name ~ type.type|id ~ '-value',
    name: name ~ '[values]['~ type.type ~']',
    elementType: type.elementType(),
    selectionLabel: type.selectionLabel,
    sources: type.sources == '*' ? null : type.sources,
    limit: 1,
    elements: element,
}) }}

