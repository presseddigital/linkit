{% import "_includes/forms" as forms %}

{{ forms.textField({
    id: name ~ type.type|id ~ '-value',
    name: name ~ '[values]['~ type.type ~']',
    value: link.value ?? '',
    placeholder: 'PLACEHOLDER ??',
    errors: link.getErrors('value') ?? null
}) }}

